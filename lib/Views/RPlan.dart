import 'package:flutter/material.dart';

class RPlan extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Center(
        child: Text("RPlanView"),
      ),
    );
  }

}