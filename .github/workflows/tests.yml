name: Tests

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: subosito/flutter-action@v1
      with:
        channel: 'flutter-2.8-candidate.20'
    - run: flutter analyze
    - run: flutter test
